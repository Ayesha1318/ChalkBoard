
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ChalkBoard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<link rel="stylesheet" href="/classDetail.css">

<body>
  <div class="app-container">
   <%- include('./partial/nav') %>
   <div id="whole-sec">
   <%- include('./partial/sidebar') %>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f7fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .container {
      width: 500px;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      overflow: hidden;
      position: relative;
      width: 100%;
    }

    .toggle-buttons {
      display: flex;
    }

    .toggle-buttons button {
      flex: 1;
      padding: 15px;
      border: none;
      cursor: pointer;
      background: #e0e0e0;
      font-size: 16px;
      font-weight: bold;
      transition: background 0.3s;
    }

    .toggle-buttons button.active {
      background: #c0d5ec;
      color: #fff;
    }

    .form {
      display: none;
      padding: 20px;
      animation: fadeIn 0.5s;
      text-align: center;
      width: 100%;
    }

    .form.active {
      display: block;
    }

    .folder {
      background: #f0f4ff;
      border: 2px solid #007BFF;
      border-radius: 8px;
      padding: 20px;
      text-align: left;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .folder h3 {
      margin: 0 0 10px 0;
      color: #007BFF;
    }

    @keyframes fadeIn {
      from {opacity: 0;}
      to {opacity: 1;}
    }
.file-item {
  display: flex;
  align-items: center;
  gap: 10px;       
  margin: 6px 0;
  width: fit-content; 
}


.dropdown {
  position: relative;
  display: inline-block;
}

.dropbtn {
  background: none;
  border: none;
  cursor: pointer;
  font-size: 18px;
}

.dropdown-content {
  display: none;
  position: absolute;
  left: 100%;  
  top: 0;      
  background-color: #f9f9f9;
  min-width: 160px;
  box-shadow: 0px 4px 6px rgba(0,0,0,0.2);
  border-radius: 6px;
  z-index: 1;
}


.dropdown-content a {
  color: #333;
  padding: 8px 12px;
  text-decoration: none;
  display: block;
}

.dropdown-content a:hover {
  background-color: #eee;
}

.dropdown:hover .dropdown-content {
  display: block;
}
a{
  text-decoration: none;
color: black;
}
  </style>
</head>
<body>
  <div class="container">
    <div class="toggle-buttons">
      <button id="btn1" class="active" onclick="toggleForm('resources')">Resources</button>
      <button id="btn2" onclick="toggleForm('test')">Test</button>
    </div>

    <div id="resources" class="form active">
      <div class="folder">
        <h3>📁 Resource Folder</h3>
        <p>Here are your study materials, notes, and important links stored in one place.</p>
        <ul>
       <li class="file-item">
        <a href="/files/leaderyou.pdf" target="_blank" > 📄 Notes.pdf</a>
        <div class="dropdown">
          <button class="dropbtn">▼</button>
          <div class="dropdown-content">
            <a href="#">Generate Quiz</a>
            <a href="#">Generate Summary</a>
          </div>
        </div>
      </li>
       <li class="file-item">
       
         <a href="/files/leaderyou.pdf" target="_blank" >  📄 Sample.pdf</a>
        <div class="dropdown">
          <button class="dropbtn">▼</button>
          <div class="dropdown-content">
            <a href="#">Generate Quiz</a>
            <a href="#">Generate Summary</a>
          </div>
        </div>
      </li>
          
        </ul>
      </div>
    </div>

  
    <div id="test" class="form">
      <h2>📝 Test Section</h2>
      <p>Here you can attempt practice questions and mock tests.</p>
    </div>
  </div>

  <script>
    function toggleForm(formId) {

      document.querySelectorAll('.form').forEach(f => f.classList.remove('active'));
      
      document.querySelectorAll('.toggle-buttons button').forEach(b => b.classList.remove('active'));

      document.getElementById(formId).classList.add('active');
      
      if(formId === 'resources') {
        document.getElementById('btn1').classList.add('active');
      } else {
        document.getElementById('btn2').classList.add('active');
      }
    }
  </script>




   </div>
   </div>

</body>
</html>